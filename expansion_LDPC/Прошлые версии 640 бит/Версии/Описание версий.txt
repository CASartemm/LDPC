V2.2 - основной код самостоятельная логика , тестбентч подает данные на вх. отсутсвует axi stream в осн.коде 

V2.3 - каким то чудом получился идентичный результат с матлабом , архитекторнуое описание 

V2.4 - исправление небольших ошибок в коде , общая оптимизация 

V2.5 - создал айпи ядро , неверный выход данных  , возможно не там лежат файлы для чтения 

V2.6 - исправные результаты на выходе , тестбентч работает через айпи ядро , сомннения что за 1 такт обрабатывается один входной бит (в симуляции не вижу этого)


AXI_STREAM_V1.0 - протокол axi_stream , с счетчиком до 640 (вроде верный )

AXI_STREAM_V1.1 - протокол axi_stream  с передачей своей последовательности 640 бит 

AXI_STREAM V1.2 - S_axis принимает 640 бит , после приема начинается выдача по М линии



shift_register_processor V1.0 - отдельный модуль для генерации 640х128

LDPC V1.0 - архитектурное описание модулей подачи 640 бит и генерации матрица , умножение бит на строки с хор . не работает . 

bit _receiver V1.0 - новая версия для подачи 640 бит , 1 бит за 1 такт , подача из файла с битами ,кажется все исправно работает 

calculate_vector V1.0  вроде все верно считает , еще нет реализации mux

calculate_vector_V1.2 добавил enable для генерации матрицы 